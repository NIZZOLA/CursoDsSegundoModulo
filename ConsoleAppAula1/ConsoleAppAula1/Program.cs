using System;

namespace ConsoleAppAula1
{
    class Program
    {
        static void Main(string[] args)
        {
            /*
            int numeroInteiro1;
            numeroInteiro1 = 50;

            int numeroInteiro2 = 100;

            double numeroDouble1 = 100.5;

            numeroDouble1 = 180.50;

            decimal numeroDecimal1 = 100.5M;

            numeroDecimal1 = 233.33M;

            char letra = 'A';

            bool resultado = true;
            resultado = false;

            string nome = "MARIA JOSE DA SILVA SANTOS";

            DateTime nascimento = DateTime.Parse("01/01/2000");

            nascimento = DateTime.Parse("01/01/2000 18:59:59");

            // operações matemáticas  com tipos iguais  (int + int)
            numeroInteiro1 = numeroInteiro1 + numeroInteiro2;

            // variável destino do tipo Int, requer operações matemáticas com conversão de tipos  (int + double )
            numeroInteiro1 = numeroInteiro1 + Convert.ToInt32(numeroDouble1);

            // conversão tipando a variável com Cast
            numeroInteiro1 = numeroInteiro1 + (int)numeroDouble1;

            // a variavel do tipo double aceita sem conversão (int + double)
            numeroDouble1 = numeroInteiro1 + numeroDouble1;


            // variável destino do tipo Int, requer operações matemáticas com conversão de tipos (int + decimal)
            numeroInteiro1 = numeroInteiro1 + Convert.ToInt32(numeroDecimal1);
            // decimal com cast
            numeroInteiro1 = numeroInteiro1 + (int)numeroDecimal1;

            var decimalNovo = numeroInteiro1 + numeroDecimal1;

            numeroDecimal1 = numeroInteiro1 + numeroDecimal1;

            // a variavel do tipo decimal aceita sem conversão (int + decimal)
            numeroDecimal1 = numeroInteiro1 + numeroDecimal1;

            // conversão de double para decimal
            numeroDecimal1 = numeroInteiro1 + (decimal)numeroDouble1;
            numeroDecimal1 = numeroInteiro1 + Convert.ToDecimal(numeroDouble1);

            // conversão de decimal para double
            numeroDouble1 = numeroInteiro1 + (double)numeroDecimal1;
            numeroDouble1 = numeroInteiro1 + Convert.ToDouble(numeroDecimal1);



            // concatenação de strings
            nome = nome + " " + numeroDouble1;

            var nome2 = string.Format(" {0} = R$ {1} ", nome, numeroDecimal1);

            var nome3 = string.Concat(nome, numeroDecimal1);
            
            Console.WriteLine(nome3);

         //   var nome = "MARCIO ROGERIO NIZZOLA";

            var nota = 10;

            nota = nota / 3;

            Console.WriteLine("Nota:" + nota);
            */


        }
    }
}
